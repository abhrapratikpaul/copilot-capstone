# Development Code

This directory contains the Python development code for the application.

## Structure
- `src/` — Source code
- `tests/` — Unit and integration tests

## Setup
```bash
cd dev
pip install -e ".[dev]"
```

## Running Tests
```bash
cd dev
pytest
```
